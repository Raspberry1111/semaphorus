[package]
name = "sephamorus"
version = "0.1.0"
edition = "2021"

[features]
nightly = [] # Uses negative_impls instead of PhantomData<*mut ()>
std = [] # Uses std::thread::sleep and std::error::Error
default = ["std"]

[dependencies]
